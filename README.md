# research-reward-aligned-colbert
eward-aligned retrieval over frozen, compressed ColBERT indexes. This project trains a lightweight query-side adapter using downstream QA rewards without re-encoding documents or rebuilding indexes, and evaluates retrieval quality, RAG answer accuracy, compression, latency, and storage cost.
